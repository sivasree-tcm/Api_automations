{
  "scenario": "Edit Project dynamic update",
  "testCases": [
    {
      "tcId": "EDIT_PROJ_01",
      "name": "Update Project Dates and Sync Store",
      "role": "Admin",
      "request": {
        "projectId": "{{dynamic}}",
        "userId": "{{dynamic}}",
        "projectName": "{{dynamic}}",
        "projectStartDate": "{{dynamic}}",
        "projectEndDate": "{{dynamic}}",
        "storageType": "{{dynamic}}",
        "webFramework": "{{dynamic}}"
      }
    }
  ]
}