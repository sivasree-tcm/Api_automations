trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  # 1️⃣ Run API automation tests
  - task: Maven@3
    displayName: 'Run API Automation Tests'
    inputs:
      mavenPomFile: 'pom.xml'
      goals: 'clean test'
      publishJUnitResults: true
      testResultsFiles: '**/surefire-reports/TEST-*.xml'

  # 2️⃣ Debug – know exactly what is generated
  - script: |
      echo "===== TARGET FOLDER STRUCTURE ====="
      ls -R target || true
    displayName: 'Debug: List target folder'

  - task: CopyFiles@2
    inputs:
      SourceFolder: '$(Build.SourcesDirectory)/target/report'
      Contents: '**'
      TargetFolder: '$(Build.ArtifactStagingDirectory)/Custom_Report'

  - task: PublishBuildArtifacts@1
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/Custom_Report'
      ArtifactName: 'CustomApiDashboard'
