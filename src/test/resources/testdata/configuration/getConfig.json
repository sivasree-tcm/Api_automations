{
  "scenario": "Get Project Config API",
  "testCases": [
    {
      "tcId": "GET_CONFIG_VALID",
      "name": "Fetch configuration for project",
      "role": "SUPER_ADMIN",
      "authType": "VALID",
      "expectedStatusCode": 200,
      "request": {
        "userId": "{{userId}}",
        "projectId": "{{projectId}}"
      }
    }
  ]
}